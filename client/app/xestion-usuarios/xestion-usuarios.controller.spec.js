'use strict';

describe('Controller: XestionUsuariosCtrl', function () {

  // load the controller's module
//  beforeEach(module('minervaApp'));
//
//  var XestionUsuariosCtrl, scope;
//
//  // Initialize the controller and a mock scope
//  beforeEach(inject(function ($controller, $rootScope) {
//    scope = $rootScope.$new();
//    XestionUsuariosCtrl = $controller('XestionUsuariosCtrl', {
//      $scope: scope
//    });
//  }));

  it('should ...', function () {
    expect(1).toEqual(1);
  });
});
