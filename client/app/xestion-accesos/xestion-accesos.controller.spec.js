'use strict';

describe('Controller: XestionAccesosCtrl', function () {

  // load the controller's module
//  beforeEach(module('minervaApp'));
//
//  var XestionAccesosCtrl, scope;
//
//  // Initialize the controller and a mock scope
//  beforeEach(inject(function ($controller, $rootScope) {
//    scope = $rootScope.$new();
//    XestionAccesosCtrl = $controller('XestionAccesosCtrl', {
//      $scope: scope
//    });
//  }));

  it('should ...', function () {
    expect(1).toEqual(1);
  });
});
